name: Persistent Ubuntu VPS (Pterodactyl Panel + Wings Auto-Restart)

on:
  workflow_dispatch:

jobs:
  vps:
    runs-on: ubuntu-latest
    timeout-minutes: 330

    steps:
      - name: 🧱 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🔁 Restore vm_data from vm-data branch
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          echo "🔁 Restoring vm_data..."
          rm -rf vm_data
          mkdir -p vm_data
          git clone --single-branch --branch vm-data "https://$PAT_TOKEN@github.com/${{ github.repository }}" vm_data || echo "⚠️ No previous data found."
          echo "✅ vm_data restored (if available)."

      - name: ⚙️ Install required packages
        run: |
          sudo apt-get update -y
          sudo apt-get install -y git curl unzip rsync nginx php php-fpm php-cli php-mbstring php-xml php-bcmath php-json php-mysql mariadb-server redis sudo
          echo "✅ Dependencies installed (Docker skipped)."

      - name: ⚙️ Start SSHX (Remote Access)
        run: |
          echo "⚙️ Launching SSHX..."
          cd vm_data
          nohup bash -c "curl -sSf https://sshx.io/get | sh -s run > sshx_link.txt 2>&1" &
          sleep 5
          echo "🌐 SSHX Link:"
          grep -Eo 'https://sshx.io/[a-zA-Z0-9]+' vm_data/sshx_link.txt || echo "⚠️ SSHX link not ready yet."

      - name: 🧩 Restore Pterodactyl Panel + Wings
        run: |
          echo "♻️ Restoring Pterodactyl data..."
          sudo mkdir -p /var/www/pterodactyl /etc/pterodactyl
          sudo rsync -a vm_data/panel/ /var/www/pterodactyl/ 2>/dev/null || echo "⚠️ No panel data to restore."
          sudo rsync -a vm_data/wings/ /etc/pterodactyl/ 2>/dev/null || echo "⚠️ No wings data to restore."
          echo "✅ Restore complete."

      - name: 🧱 Start Pterodactyl Panel + Wings
        run: |
          echo "🚀 Starting Pterodactyl Panel + Wings..."

          sudo systemctl enable nginx php-fpm mariadb redis-server || true
          sudo systemctl start nginx php-fpm mariadb redis-server || true
          sudo systemctl start docker || true  # Docker preinstalled in GitHub runner

          # === PANEL ===
          if [ -d "/var/www/pterodactyl" ]; then
            cd /var/www/pterodactyl
            if ! ss -ltnp | grep -q ":80"; then
              echo "🌐 Starting PHP built-in server..."
              nohup php artisan serve --host=0.0.0.0 --port=80 > ~/panel.log 2>&1 &
            else
              echo "✅ Port 80 in use — Nginx serving panel."
            fi
          else
            echo "⚠️ Panel directory not found."
          fi

          # === WINGS ===
          if [ -f "/etc/pterodactyl/config.yml" ]; then
            echo "🚀 Launching Wings..."
            nohup sudo wings --config /etc/pterodactyl/config.yml > ~/wings.log 2>&1 &
          else
            echo "⚠️ Wings config not found."
          fi

      - name: 💾 Autosave (every 60s)
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          echo "💾 Starting autosave loop..."
          nohup bash -c '
          while true; do
            echo "[ $(date "+%T") ] 🔁 Syncing panel and wings..."
            sudo mkdir -p /var/www/pterodactyl /etc/pterodactyl
            mkdir -p vm_data/panel vm_data/wings
            sudo rsync -a --delete /var/www/pterodactyl/ vm_data/panel/ 2>/dev/null || true
            sudo rsync -a --delete /etc/pterodactyl/ vm_data/wings/ 2>/dev/null || true

            cd vm_data || exit
            git init -q
            git remote remove origin >/dev/null 2>&1 || true
            git remote add origin https://$PAT_TOKEN@github.com/${{ github.repository }}
            git checkout -B vm-data >/dev/null 2>&1
            git add .
            git config user.name "github-actions"
            git config user.email "actions@github.com"
            git commit -m "💾 Auto-backup $(date '+%F %T')" >/dev/null 2>&1 || true
            git push -f origin vm-data >/dev/null 2>&1 || true
            echo "✅ Backup pushed to vm-data."
            sleep 60
          done
          ' >/dev/null 2>&1 &

      - name: 🕓 Keep VPS Running
        run: |
          echo "🕓 VPS active — sleeping until timeout..."
          sleep $((330*60 - 300))

      - name: ♻️ Final Backup + Restart Workflow
        if: always()
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          echo "💾 Finalizing backup..."
          sudo mkdir -p /var/www/pterodactyl /etc/pterodactyl
          sudo rsync -a --delete /var/www/pterodactyl/ vm_data/panel/ 2>/dev/null || true
          sudo rsync -a --delete /etc/pterodactyl/ vm_data/wings/ 2>/dev/null || true

          cd vm_data
          git init -q
          git remote remove origin >/dev/null 2>&1 || true
          git remote add origin https://$PAT_TOKEN@github.com/${{ github.repository }}
          git checkout -B vm-data >/dev/null 2>&1
          git add .
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git commit -m "💾 Final backup before restart $(date '+%F %T')" >/dev/null 2>&1 || true
          git push -f origin vm-data >/dev/null 2>&1 || true
          echo "✅ Final backup complete."

          echo "♻️ Restarting workflow..."
          curl -s -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $PAT_TOKEN" \
            "https://api.github.com/repos/${{ github.repository }}/actions/workflows/$(basename "$GITHUB_WORKFLOW").yml/dispatches" \
            -d '{"ref":"main"}' || echo "⚠️ Restart request may have failed (non-blocking)."
          echo "✅ Restart triggered."
